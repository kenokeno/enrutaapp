import React from 'react';
import { Text, Center } from 'native-base';

import Colors from './../../../constants/colors';

export default () => {
    return (
        <Center backgroundColor={Colors.TERTIARY_COLOR}>
            <Text>Inventario</Text>
        </Center>
    );
}